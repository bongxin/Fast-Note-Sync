对比的是现场原始稿 **`边轨道测试程序，26.09.10.smart`** 和当前量产 **`v1.1.9.1/plc-ladder-v1.1.9.1.smart`**。量产不是在草稿上改几个点，而是按草稿能用的部分重画，**禁止把原始稿原样下到 CPU**。

## 从草稿收进来的

- **阀名和地址不变**：Q0.0 整猪自动变轨、Q0.1 片猪自动变轨。
- **手/自动旋钮仍是 I0.3**。
- **手动点动保留**，但地址改了（见下）。
- **「申请次数」保留语义**：草稿 VW10 本地自加；量产改成 VW102，由 Java 用同一序号回写 VW202。

## 地址必须改的

| 草稿 | 量产 | 原因 |
|---|---|---|
| I0.0 识别到整猪、I0.1 识别到片猪 | I0.0 只做主轨光电；体态只看 **VB200** | 现场没有两路「识别」开关，体态在屠宰系统里 |
| I0.2 入轨/到位光电 | 无入轨光电；I0.1/I0.2 改成气缸伸出/缩回限位 | 现场没有入轨光电 |
| I0.4/I0.5 手动变轨 | **I0.4 急停、I0.5 复位**；手动改到 **I0.6/I0.7** | 草稿把急停点占了，量产不能照抄 |
| Q0.2 完成变轨灯 | **Q0.2 备用，不接线** | 不接完成灯、不接声光 |
| M0.0/M0.1 整猪位/片猪位 | M0.0=自动模式；目标轨用 M0.4/M0.5 | 只吸收「记住走哪边」，不吸收地址 |

## 草稿有、量产丢掉的

- 用两路输入当体态源（识别整猪/片猪）。
- 入轨光电 + M1.0/M1.1「到光电位」后点亮 Q0.2。
- 纯本地自动：识别 → 直接打阀，没有和屠宰系统握手。

量产流程改成：**主轨光电 ED → 写 VB100 → 等 Java 写 VB200 → 再打同一对阀**。到位靠气缸限位 + 主轨光束恢复（I0.0 上升沿）+ 3s 保持。

## 草稿没有、量产补上的

- 急停锁存、复位、气缸限位联锁。
- S7 V 区握手：VB100 / VW102 / VB200 / VW202 / VB108 心跳。
- VB200 只能是 0/1/2（NETWORK 4A）；非法值置故障。
- 通讯超时 500ms 默认整猪轨；主轨遮挡 8s 粘连报警。
- 故障只置 M1.2 + VB301，不驱动现场声光。
- Q0.3 运行灯。

手动模式：I0.6 / I0.7 点动，两路互锁，受急停切断，**不要再接到 I0.4/I0.5**。