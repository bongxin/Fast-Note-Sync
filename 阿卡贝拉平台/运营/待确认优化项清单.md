---
title: 待确认优化项清单
source: dev_memory/pending_optimizations_dev_memory.md
project: BERNSINE·阿卡贝拉
tags:
  - 阿卡贝拉
  - 方案策划
---

# 待确认优化项清单

## 开发要点
- **口径**：此处只记「方案已聊过、等确认或刻意延后」的优化；已改完只欠部署的见 `server_ssh_dev_memory`（约定改完直接部署）。

### 2026-07-24 已处理
1. **贝拉暖色全站铺开** → 已确认执行并部署（见 `brand_warm_palette_rollout_dev_memory.md`）
2. **首页 Tab / 推荐混排** → **确认不做**（见 `home_tab_ia_dev_memory.md`）
3. **短视频切条补浏览量** → 已确认执行并部署（Shorts `1.2.36`，见 `b2_shorts_dev_memory.md`）
4. **微信分享 API 升级 + 手机浏览器登录引导** → 已部署（见 `wechat_platform_dev_memory.md`）。PC 扫码仍未开。
5. **多轨练习 P0（独立「练习」频道）** → 已部署 `/practice`（见 `practice_dev_memory.md`）。
6. **/write 发布页轻负担** → 已上线 v3（白底、题图引导、发布到横滑、底栏主次 CTA）；见 `write_publish_ux_dev_memory.md`。
6. **视频 AI 解析 P0（评论区问贝拉）** → 已部署，插件 `0.4.7`（见 `video_ai_comment_proposal.md`）。
7. **模拟广告 / 广告投放** → 已部署三槽位后台管理 + 强制「广告」角标（见 `mock_ads_proposal.md`）。素材请在后台「广告投放」自行添加。
8. **语音评论 P0（按住说话）** → 已部署：文章/声场/短视频；REST + 气泡播放（见 `voice_comment_proposal.md`）。
9. **声场两组重复旧话题下架** → 已处理：保留 `4759`/`4131`，回收站 `4790`/`4885`/`4886`/`4730`（见 `ops_audit_ux_dev_memory.md`）。
10. **底栏高亮** → 已部署：非首页不再误亮「首页」（`syncFooterActive`）。
11. **工作室/录音室挂单 P0** → 已执行：演示货下架；5 个服务样板 SKU；指南《工作室如何挂单》`5037`（见 `studio_shop_orders_proposal.md`）。

### 待确认
- **bot_guard（爬虫减压）**：已 live（UA 硬拦 + fpm 降并发 + swap + robots）。可选后续仍缺：category 多 tags 限流/缓存、WAF、443/fpm 告警、闲置 php 81/83 池。详见 `incident_20260726_site_unreachable_dev_memory.md`。
- **微信 PC 扫码**：是否开 `wx_pc_open`（桌面端开放平台扫码；手机浏览器仍需引导进微信）。公众平台域名/服务器核对清单可另做。
- **练习 P1 余量**：不改调变速、会员解锁——等素材或优先级。**曲库/本周练什么/开排筛/招新贴/歌单** 已于 2026-07-29 部署（`rehearsal_p0_dev_memory`）；真人分轨替换仍待合作。
- **PDF 乐谱 → 试听音频（OMR）**：**P0 已跑通**（8/8 → mxl+WAV）。报告 `_ops_pack/score_omr_p0/P0_REPORT.md`。等你试听后确认是否进 P1（后台半自动任务）。
- **编曲/制谱人合作智能化谱**：指南已上线 `6216`《如何提交可练习谱》；等你定是否先发合作邀约。
- **视频 AI P1/P2**：本片导读缓存 / ASR（有源文件时）——暂不排期。
- **贝拉协助一键发布（认证用户）**：**方向保留**；严格评估见 `bella_assisted_publish_eval.md`。结论：勿静默直发；建议 P0=生成草稿+用户确认后打开发布表单。等你确认是否按 P0 开工。

### 已具备（无需新建）
- **文档「平台指南」= 产品手册 v3（用户向）**：`document_cat` 1305 `/document/pingtaizhinan`；侧栏 8 模块（总览/账号/浏览/内容/发布/社区/团体/帮助）。旧 FAQ 已 stub 跳转，**未**开 B2 问答。详见 `document_mobile_dev_memory.md`。
- **运营拟真/文档/回响自动巡检**：`ops_audit_ux` 已部署并挂入日更（见 `ops_audit_ux_dev_memory.md`）。
- **移动端体验评估（2026-07-24）**：总评中上；P0 中拟真重复/底栏高亮/商城演示货已处理；顶栏过密仍待做（见 `mobile_ux_audit_20260724.md`）。

### 不是漏项
- 「先不动」仅指当时正在改的退出弹窗方案，后续手势/滑动问题已按新指令改并部署
- 此前「提示部署、等确认」类缺口已在 [部署回查](577dbbde-c6a4-4a97-80af-39fa8dbe9870) 补齐，约定改完直接上线

## 改动涉及的文件
- `dev_memory/brand_warm_palette_rollout_dev_memory.md`
- `dev_memory/home_tab_ia_dev_memory.md`
- `dev_memory/b2_shorts_dev_memory.md`
- `dev_memory/server_ssh_dev_memory.md`
- `dev_memory/wechat_platform_dev_memory.md`
- `dev_memory/practice_dev_memory.md`
- `dev_memory/multitrack_practice_proposal.md`
- `dev_memory/video_ai_comment_proposal.md`
- `dev_memory/mock_ads_proposal.md`
- `dev_memory/voice_comment_proposal.md`
- `dev_memory/ops_audit_ux_dev_memory.md`
- `dev_memory/recruitment_onboarding_plan.md`
- `dev_memory/studio_shop_orders_proposal.md`
- `dev_memory/bella_assisted_publish_eval.md`
- `dev_memory/mobile_ux_audit_20260724.md`
- `dev_memory/incident_20260726_site_unreachable_dev_memory.md`

---

> 源文件：`dev_memory/pending_optimizations_dev_memory.md`（仓库同步；改文请回仓库或再跑同步脚本）

